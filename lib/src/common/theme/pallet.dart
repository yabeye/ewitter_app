import 'package:flutter/material.dart';

class KPalette {
  // App Colors
  static const Color primaryColor = Color(0XFFD58F77);
  static const Color primaryColorLight = Color(0XFFEDC0AD);
  static const Color secondaryColor = Color(0XFFF5DFCE);

  static const Color backgroundColor = Colors.black;
  static const Color searchBarColor = Color.fromRGBO(32, 35, 39, 1);

  // Basic colors!
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color redColor = Color(0xFFF9197F);
  static const Color greenColor = Colors.green;
}
